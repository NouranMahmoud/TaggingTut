$('#my-listings').append('<%= j render @listings %>');
<% if @listings.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @listings %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>